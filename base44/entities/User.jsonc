{
  "name": "User",
  "type": "object",
  "properties": {
    "Company": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "Fisher's Enterprise",
          "Pencroft Structures"
        ]
      },
      "minItems": 0,
      "description": "Companies this user has access to. If empty, user has access to all companies."
    }
  },
  "required": [
    "Company"
  ]
}